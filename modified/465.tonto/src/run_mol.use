   use TYPES_MODULE
   use SYSTEM_MODULE
 
   use TEXTFILE_MODULE, only: stdin
   use TEXTFILE_MODULE, only: stdout
   use TEXTFILE_MODULE, only: create_stdout_
   use TEXTFILE_MODULE, only: open_
   use TEXTFILE_MODULE, only: create_stdin_
 
   use PARALLEL_MODULE, only: finalise_
   use PARALLEL_MODULE, only: initialise_
 
